{
  "config": {
    "step": {
      "user": {
        "title": "Cookidoo Today",
        "description": "Configure the backend API connection.",
        "data": {
          "base_url": "Base URL",
          "today_path": "Today endpoint path",
          "verify_ssl": "Verify SSL",
          "timeout": "Timeout (seconds)"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to the backend.",
      "invalid_response": "Backend returned invalid response.",
      "unknown": "Unexpected error."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Cookidoo Today options",
        "data": {
          "base_url": "Base URL",
          "today_path": "Today endpoint path",
          "verify_ssl": "Verify SSL",
          "timeout": "Timeout (seconds)"
        }
      }
    }
  }
}
